# Analysis / scratch artifacts — not part of the site
_raw/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Node deps (temp tooling only, not part of the site)
node_modules/
package.json
package-lock.json
